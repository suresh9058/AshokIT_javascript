let fruits = ["apple","orange","graphes"];

fruits.push("pear");

console.log(fruits);

fruits.pop();

console.log(fruits);

fruits.unshift('banana'); // add item to the begining of the array
console.log(fruits);

fruits.shift(); // remove the item at the begining of the array
console.log(fruits);













